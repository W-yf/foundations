# foundations 协作规则

本仓库负责 R0 短准备的学习成果。总路线与统一复习由 [self-Learning 主仓库](https://github.com/W-yf/self-Learning) 维护；课程范围、验收、当前状态与下一步见 [README](README.md)。

## 开始与接续

1. 每次开始先核对本课程和主仓库的目录、分支、上游及本地修改。使用当前机器已关联的实际目录，不假定两个仓库互相嵌套或具有固定相对路径。
2. 有远程时按已获授权范围优先快进拉取；有未同步修改或分叉时先核对，保留已有内容。无远程或未能拉取时明确说明，不记作已同步。
3. 显式读取主仓库 AGENTS、README、STATUS、R0 计划和资源，再读本课程 README、相关笔记与实验。不要依赖跨仓库自动发现规则或其他 session 的记忆。
4. 正式学习前读取主仓库 REVIEW 与题库，一次只提出一道到期题，等待回答后继续。课程仓库不另建竞争的复习队列。
5. 接手已有 handoff 时核对日期、基线提交及未同步内容。最新可核对的文档与证据优先，旧快照不能覆盖新事实。

## 学习与证据

- 理论与实验分别验收。引导用户独立解释、推导、实现和验证；根据用户需要提供帮助。AI 生成结果不记作用户独立掌握；知识笔记不附逐题提示过程或掌握评语。
- 统一层级：1. 看过；2. 能够解释；3. 能够独立实现；4. 能够分析正确性与性能。无相应证据不提高层级，不根据一次答对推断完整掌握。
- 初始化、文档整理和工具安装不计作学习日或课程知识掌握。环境安装与验证、测试通过与独立理解分别记录。
- 遵守 README 的 R0 范围，工具按当前任务补齐；图接口决策、两种表示和契约测试属于学习任务，不在初始化时预填实现或答案。
- `notes/` 只记录课程知识和用户对知识的理解，保持精简、准确，必要时附权威来源、适用边界与例子；不保存问答流水、作答原文、提示过程或教学评语，不把 AI 补充冒充用户的个人理解。`exercises/` 保存实验代码、必要的运行方法与验证结果。`logs/` 仅按需保存环境、同步和重大计划变更，不逐题记录学习过程。
- 性能实验先验证正确性，保留编译配置、重复次数和计时范围；C++ 实验关注生命周期、所有权及指针、引用、迭代器失效。

## 记录与同步

- 学习时专注阅读、讨论和练习，不每答一题就修改文件。完成一个小节后整理知识笔记，更新 README 的必要进度与下一步；本轮结束或交接时回写主仓库摘要，不重复记录详细学习过程。课程结束时统一核对来源、合并重复内容、精简结构。
- 学习结束逐仓库审阅；在获得相应授权后先同步并核对课程成果，再回写、同步并核对主仓库。无提交、未推送、无远程或汇总待更新均如实记录。
- 默认通过 Git 接续：换电脑后拉取主仓库与课程仓库，读取主仓库 STATUS 和本课程 README 即可定位当前任务，不依赖临时文件或聊天记忆。只有用户另行要求时才使用指定 handoff skill 生成补充快照；该快照不能覆盖更新的仓库状态。
- 当前会话能访问两仓库且已获同步授权时，可直接完成课程发布和主仓库回写，无需用户切换管理会话。发布后逐仓库核对远程提交；未同步内容明确标记。

## Git 与公开边界

- Git 操作显式指定目标仓库目录，操作前后检查状态；同一 Codex 项目关联多个目录不等于它们共用 Git 历史。
- 未获明确授权不执行 git add、git commit、git push，不创建远程仓库、不擅改远程、强推、重写历史或破坏性清理。用户已授权的具体操作无需重复确认。
- 提交前审阅实际内容与暂存区并运行 git diff --check。忽略规则不替代公开内容检查。
- 只保存独立编写、公开且许可明确的内容。不得读取、复制或提交公司代码、内部文档、客户数据、凭据或不确定能否公开的材料；公开文档不含私人绝对路径、用户名、设备名或内部环境信息。
- 课程资料和代码使用前核对许可；受限框架、测试或解答不能因放入被忽略目录而被当作已获使用或公开授权。
